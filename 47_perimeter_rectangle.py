# 47_perimeter_rectangle:   

def perimeter_rectangle(length,width):
    length_perimetre = length * 2
    width_perimetre = width * 2
    total_permienter = length_perimetre + width_perimetre
    print(total_permienter)

perimeter_rectangle(2,9)
